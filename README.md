# Автоматизація інфраструктури AWS: VPC та EKS Cluster

Цей проєкт присвячений створенню масштабованої та відмовостійкої інфраструктури для ML-сервісів за допомогою **Terraform**. Проєкт реалізує автоматизоване розгортання мережевого середовища (VPC) та керованого кластера Kubernetes (EKS) з використанням офіційних модулів AWS.

## Архітектура проєкту

Інфраструктура розділена на два логічні модулі для кращого керування стейтом та ресурсами:
1.  **VPC Module**: Створює ізольовану мережу в регіоні `eu-central-1` з публічними та приватними підмережами, NAT Gateway та Internet Gateway.
2.  **EKS Module**: Розгортає Kubernetes-кластер у створеній VPC. Кластер використовує `terraform_remote_state` для отримання ідентифікаторів мережі.



---

## 📂 Структура репозиторію

```text
eks-vpc-cluster/
├── main.tf              # Кореневий файл для виклику модулів vpc та eks
├── variables.tf         # Глобальні змінні проєкту
├── outputs.tf           # Глобальні вихідні дані
├── terraform.tf         # Налаштування провайдерів (AWS, Kubernetes)
├── backend.tf           # Налаштування S3 Backend для зберігання стейту
├── vpc/                 # Модуль мережі
│   ├── main.tf          # Виклик модуля terraform-aws-modules/vpc/aws
│   ├── variables.tf
│   ├── outputs.tf
│   ├── terraform.tf
│   └── backend.tf
└── eks/                 # Модуль Kubernetes
    ├── main.tf          # Виклик модуля terraform-aws-modules/eks/aws
    ├── variables.tf
    ├── outputs.tf
    ├── terraform.tf
    └── backend.tf
